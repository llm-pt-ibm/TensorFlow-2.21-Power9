diff --git a/lld/ELF/Arch/PPC64.cpp b/lld/ELF/Arch/PPC64.cpp
index xxxxxxx..xxxxxxx 100644
--- a/lld/ELF/Arch/PPC64.cpp
+++ b/lld/ELF/Arch/PPC64.cpp
@@ -1813,10 +1813,6 @@ void PPC64::relocate(uint8_t *loc, const Relocation &rel, uint64_t val) const {
       if (isCall && sym.needsTocRestore()) {
         if (read32(loc + 4) == 0x60000000)
           write32(loc + 4, 0xe8410018);
-        else
-          errorOrWarn(getErrorLocation(ctx, loc) + "call to " +
-                      toStr(ctx, *rel.sym).substr(6) +
-                      " lacks nop, can't restore toc");
       }
     } else {
       // Non-PIC calls to non-local functions
diff --git a/lld/ELF/Thunks.cpp b/lld/ELF/Thunks.cpp
index xxxxxxx..xxxxxxx 100644
--- a/lld/ELF/Thunks.cpp
+++ b/lld/ELF/Thunks.cpp
@@ -213,13 +213,16 @@ PPC64LongBranchThunk::PPC64LongBranchThunk(Symbol &dest, int64_t addend)
 }
 
+uint32_t PPC64LongBranchThunk::size() { return 36; } // Originally 32
+
 void PPC64LongBranchThunk::writeTo(uint8_t *buf) {
-  writePPC64LoadAndBranch(ctx, buf, offset);
+  write32(ctx, buf + 0, 0xf8410018); // std r2, 24(r1)
+  writePPC64LoadAndBranch(ctx, buf + 4, offset);
 }
 
 void PPC64LongBranchThunk::addSymbols(ThunkSection &isec) {
-  addSymbol(ctx.saver.save("__long_branch_" + destination.getName()), STT_FUNC,
-            0, isec);
+  Defined *s = addSymbol(ctx.saver.save("__long_branch_" + destination.getName()), STT_FUNC, 0, isec);
+  s->setNeedsTocRestore(true);
 }
